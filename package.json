{
  "name": "ondemand-performance-recommendation-api",
  "version": "2.0.0",
  "description": "An API that allows an external agent to recommend a change to an OnDemand service. In this version, only bandwidth changes are supported. Note that this API requires the caller to first login and obtain a token. The token should then be presented with each API call in the Authentication header, with type Bearer. ",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "node index.js"
  },
  "keywords": [
    "swagger"
  ],
  "license": "Unlicense",
  "private": true,
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "jsonwebtoken": "^8.3.0",
    "swagger-tools": "0.10.1"
  }
}
